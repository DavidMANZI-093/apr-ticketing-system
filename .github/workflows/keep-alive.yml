name: Keep Alive
on:
  schedule:
    - cron: '* * * * *' # every minute
  workflow_dispatch: # Allow manual trigger
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -f ${{ secrets.API_BASE_URL }}/health